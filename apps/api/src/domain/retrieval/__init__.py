"""Retrieval Domain Package.

Coordinates parallel search queries (dense vector and sparse BM25 text indices),
metadata filter overlays, reciprocal rank fusion (RRF), and cross-encoder relevance rerank models.
"""
