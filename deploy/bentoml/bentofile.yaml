service: "deploy.bentoml.service:RetrieverVllmService"
labels:
  owner: retriever-team
  project: retriever-serverless-gpu
  milestone: M96
include:
  - "deploy/bentoml/service.py"
python:
  packages:
    - "vllm>=0.6.0"
    - "torch>=2.4.0"
    - "transformers>=4.44.0"
    - "peft>=0.12.0"
    - "fastapi>=0.110.0"
    - "uvicorn>=0.28.0"
    - "pydantic>=2.6.0"
docker:
  cuda_version: "12.4"
  system_packages:
    - "git"
    - "curl"
