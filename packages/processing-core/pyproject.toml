[project]
name = "processing-core"
version = "0.1.0"
description = "Shared document processing primitives for Retriever"
requires-python = ">=3.11"
dependencies = [
    "pdfplumber>=0.11.0",
    "tiktoken>=0.6.0",
]

[tool.ruff]
line-length = 88
target-version = "py311"
